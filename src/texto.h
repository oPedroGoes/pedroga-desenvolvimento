#ifndef texto_h
#define texto_h

//NÃO FINALIZADO.
    
    typedef void* TEXTO;

    TEXTO cria_texto(int i, float x, float y, char* cb, char* cp, char a, char* txt, char* ff, char* fw, char* fs);

#endif